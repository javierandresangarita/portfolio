module.exports = {
    content: [
      "./src/**/*.{html,js,jsx,ts,tsx}",
      // Make sure App.css is not directly required here unless it's inside the content paths
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  